# v0.1.14

**Date:** 2026-08-02
**Bump:** patch (from v0.1.13)

## Notes

Voice input: dictate tasks into the Add Task modal (local Whisper). Comments now show newest first with the input box on top. Desktop: notification stream reconnects with backoff. Runner: swapped --dangerously-skip-permissions for --permission-mode auto, resumes cleanly when a session id is no longer found, and a user cancel now pauses the task instead of burning retries. Fixes: usage state soft-resets on account swap; scheduler no longer broadcasts while holding its lock; two UI infinite-render loops squashed.

## Build

- Target: darwin-aarch64
- Status: built
